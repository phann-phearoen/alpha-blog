<h1 class="text-center mt-4">Articles list</h1>

<% if logged_in? %>
    <%= link_to 'Create new article', new_article_path, class: "btn btn-primary ms-5" %>
<% end %>

<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
</div>

<%= render 'article-card' %>

<div class="flickr_pagination my-3">
    <%= will_paginate @articles, :container => false %>
</div>